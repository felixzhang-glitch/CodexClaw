#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

if [[ $# -eq 0 ]]; then
  exec "$ROOT_DIR/bin/server" start
fi

if [[ "$1" == "-f" || "$1" == "--foreground" ]]; then
  exec "$ROOT_DIR/bin/server" start -f
fi

exec "$ROOT_DIR/bin/server" "$@"
